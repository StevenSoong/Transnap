# 更新记录

本项目遵循 Semantic Versioning（语义化版本，用主版本、次版本和修订版本表达兼容性变化）。

## 1.10.0

- 整理源码目录为 `App`、`Core`、`UI` 和 `Support`。
- 新增标准化 README、贡献指南、安全策略、许可证和持续集成配置。
- 将公开默认接口改为 `https://api.openai.com/v1`。
- 移除内部接口地址和内部环境变量名称。
- 将接口密钥从应用偏好设置迁移到仅当前用户可读写的本机私有文件。
- 新增不读取真实密钥或翻译历史的文档截图模式。
- 保留选区就近显示、多屏定位、翻译历史和自定义快捷键等现有功能。
