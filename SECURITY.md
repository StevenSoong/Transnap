# 安全策略

## 支持范围

安全修复优先应用于最新发布版本。请先在最新版中确认问题是否仍然存在。

## 报告安全问题

请使用 GitHub 的 Private vulnerability reporting（私密漏洞报告）功能联系维护者，不要创建公开 Issue（问题单）。报告中可以包含：

- 受影响版本
- 复现步骤
- 影响范围
- 建议修复方式

请勿发送真实接口密钥、未脱敏的翻译历史或其他用户隐私数据。若必须提供请求示例，请使用无效的占位密钥和公开测试文本。

## 密钥处理边界

Transnap 不在源码中提供密钥。用户密钥只保存在本机私有配置文件中，并通过 `Authorization` 请求头发送到用户自己配置的模型服务。维护者不会要求用户在 Issue、讨论或 Pull Request 中提供真实密钥。
